import express from 'express';
import '../controllers/transactionsControllers.js';

const transactionsRouter = express.Router;

// router.get('/transactions', controllers.transaction);

export default transactionsRouter;
